# scrapers/.env.example
CRM_API_URL=https://your-crm.vercel.app/api/listings
SCRAPER_API_KEY=crm_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx   # gerada em Dashboard → Definições → API Keys
MAXWORK_EMAIL=elsiomota@remax.pt
MAXWORK_PASSWORD=a-tua-password
HEADLESS=true
# Opcional — vários links (separados por vírgula), para percorrer várias
# vistas/filtros do Maxwork numa só corrida do maxwork_to_csv.py. Se
# deixares vazio, usa só a lista de imóveis do escritório por omissão.
MAXWORK_URLS=https://app.maxwork.pt/listing/officelistings
